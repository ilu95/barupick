// ================================================================
// versions.ts — 점수 엔진·팔레트 버전 (기록과 계측에 함께 남긴다)
//
// 결정(2026-09-08): "점수 계산은 기존과 100% 동일" 제약을 풀고, 대신
// 저장되는 코디마다 어떤 엔진·팔레트로 매긴 점수인지 남긴다.
// 과거 기록의 점수는 다시 계산하지 않는다.
// ================================================================

/** 색 점수 엔진. v6 = HCL 표 기반 evaluation.ts (현행). v7.1 이식 시 'v7.1'. */
export const ENGINE_VERSION = 'v6'

/** 팔레트 hex 판. 1 = 웹 기본색 hex. 2 = 의류색 보정 hex(50색). */
export const PALETTE_VERSION = 1
